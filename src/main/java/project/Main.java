package project;

public class Main {
    /**
     * data structure A of ParkingViolations given by either JsonReader or CsvReader
     * data structure B of PropertyValues given by CsvReader.method that reads property values
     * HashMap C given by TextReader
     *
     * ProjectData pData = new ProjectData(A, B, C)
     *
     * In order to pass the data to each processor that corresponds to the
     * option the user picks
     *
     * for example, option 2:
     * processorOfOption2(pData)
     * processorOfOption2.finesPerCap()
     */
}

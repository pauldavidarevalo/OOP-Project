package project.data;

public class TextReader {
    /** reader to parse population.txt into a HashMap mapping Zip -> Population
     * return this HashMap
     * make the method static so no instance is needed and with filename as parameter
     */
    public static HashMap <Integer, Integer >(String filename){
        /** Turn strings to ints? or keep them strings?*/
    }
}
